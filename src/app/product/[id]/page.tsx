import IdProducts from '@/feature/landingpage/IdProducts';
import styles from './styles.module.scss';
const IdPage: React.FC = () => {
  return (
    <div className={styles.container}>
      <IdProducts />
    </div>
  );
};

export default IdPage;
